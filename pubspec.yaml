name: tugasbesar_berita
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_dotenv: ^5.0.2
  http: ^1.0.0
  google_fonts: ^4.0.5
  jiffy: ^6.1.0
  loadmore: ^2.1.0
  fluttertoast: ^8.2.2
  url_launcher: ^6.1.5
  connectivity_plus: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/.env
    - assets/images/